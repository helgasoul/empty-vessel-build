
import { LucideIcon } from "lucide-react";

export interface AssessmentType {
  title: string;
  displayName: string;
  description: string;
  tooltip: string;
  icon: LucideIcon;
  color: string;
  bgColor: string;
  validation: string;
  accuracy: string;
  tab: string;
}
